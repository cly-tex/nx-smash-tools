[workspace]
members = ["binaries/*", "lib/*"]
resolver = "3"

[workspace.dependencies]
nx = { path = "lib/nx" }
rtld = { path = "lib/rtld" }
services = { path = "lib/services" }

[profile.release]
panic = "abort"
lto = true
